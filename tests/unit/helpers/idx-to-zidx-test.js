import { idxToZidx } from '../../../helpers/idx-to-zidx';
import { module, test } from 'qunit';

module('Unit | Helper | idx to zidx');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = idxToZidx(42);
  assert.ok(result);
});
