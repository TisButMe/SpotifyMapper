import { idxToPos } from '../../../helpers/idx-to-pos';
import { module, test } from 'qunit';

module('Unit | Helper | idx to pos');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = idxToPos(42);
  assert.ok(result);
});
