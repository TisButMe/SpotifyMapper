import { capitalizedName } from '../../../helpers/capitalized-name';
import { module, test } from 'qunit';

module('Unit | Helper | capitalized name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = capitalizedName(42);
  assert.ok(result);
});
